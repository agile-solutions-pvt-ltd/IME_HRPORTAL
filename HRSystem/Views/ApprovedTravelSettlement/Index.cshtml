@model IEnumerable<ApprovedTravelVoucher.approvedtravelvoucher>

@{
    ViewData["Title"] = "Approved Travel Settlements";
}

@section StyleSheets {
    <link href="~/lib/DataTables/datatables.min.css" rel="stylesheet" />
}

<section class="panel">
    <header class="panel-heading">
        <h2 class="panel-title">
            Approved Travel Settlement
        </h2>
    </header>

    <div class="panel-body">
        <div class="table-responsive">
            <table class="table table-bordered table-hover mb-none" id="tbl_ApprovedTravelSettlement">
                <thead>
                    <tr>
                        <th>
                            Travel Order No
                        </th>
                        <th>
                            Departure Date
                        </th>
                        <th>
                            Arrival Date
                        </th>
                        <th>
                            Recommended Date
                        </th>
                        <th>
                            Verified Date
                        </th>
                        <th>
                            Approved Date
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Travel_Order_Form_No)
                            </td>
                            <td>
                                @item.Depature_Date_AD.ToShortDateString()
                            </td>
                            <td>
                                @item.Arrival_Date_AD.ToShortDateString()
                            </td>
                            <td>
                                @item.Recommended_Date.ToShortDateString()
                            </td>
                            <td>
                                @item.Verfied_Date.ToShortDateString()
                            </td>
                            <td>
                                @item.Approved_Date.ToShortDateString()
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</section>

@section Scripts {
    <script src="~/lib/DataTables/datatables.min.js"></script>

    <script type="text/javascript">
        (function () {

            'use strict';

            var datatableInit = function () {
                $('#tbl_ApprovedTravelSettlement').dataTable({
                    "order": [[0, "desc"]]
                });
            };

            $(function () {
                datatableInit();
            });

        })();
    </script>
}
